# ══ 实验室私有数据 —— 任何一条漏出去都是实打实的泄漏 ══════════════
#
# 这个仓库是公开版。以下路径都是本实验室的内部资料。
#
# ⚠️ 改这个文件时注意：gitignore **不支持行尾注释**。
#    「path/   # 说明」会被当成一个包含空格和井号的模式串，等于没写这条规则。
#    注释只能单独占一行。改完务必跑一次 `git status --short` 确认。

# 内置的 172 条货架数据（含真实货位号、品牌、溶剂）
js/inventory-data.js

# 生成它的中间产物
tools/inventory-data.json
tools/inventory-mw-cache.json
_inventory_mw.json

# 原始 Excel 库存表。这个项目不需要任何 Excel（导入走粘贴和 CSV），
# 所以直接全挡 —— 不必逐个文件名列举，也就少一处要维护的地方
*.xls*

# 跑脚本留下的日志（逐条试剂名 + 货位号）
_*.log

# ══ 内部文档 ════════════════════════════════════════════════════

# 项目记忆：含库存设计与部署细节、内网 IP
.claude/

需求文档.txt
使用指南.md
使用指南.pdf

# 含本实验室的私有部署地址
guide.html

# ══ 只服务本实验室工作流的脚本 ══════════════════════════════════
# 它们硬编码了本实验室库存表的文件名和工作表结构，
# 对其他人没用，留着只会暴露内部结构

tools/make-inventory.py
tools/fetch-inventory.py
tools/verify-inventory.py
tools/__pycache__/

# 打包脚本：公开版本身就是可发布的站点（没有构建步骤），这个脚本只服务
# 「从私有目录里剔掉内置数据」那一个场景，而且它的泄漏扫描规则列表里
# 写着本实验室的货架分组名 —— 两样都不该进公开仓库
tools/make-dist.js

# Claude Code 的项目指令：内容是指向 .claude/memory/ 的，而那个目录被屏蔽了
CLAUDE.md

# ══ 其他 ════════════════════════════════════════════════════════

# 构建产物（make-dist.js 生成，可随时重建）
配液工具-发布/
配液工具-公开/

node_modules/
.DS_Store
Thumbs.db
